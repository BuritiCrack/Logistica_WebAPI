@if (TotalPages > 1)
{
    <nav aria-label="Pagination">
        <ul class="pagination justify-content-center">
            @foreach (var link in links)
            {
                <li class="page-item @(link.IsCurrent ? "active" : "") @(!link.IsEnable && !link.IsCurrent ? "disabled" : "")">
                    @if (link.IsEnable || link.IsCurrent)
                    {
                        <button class="page-link" @onclick="() => InternalSelectedPage(link)" disabled="@(!link.IsEnable && !link.IsCurrent)">
                            @link.Text
                        </button>
                    }
                    else
                    {
                        <span class="page-link">@link.Text</span>
                    }
                </li>
            }
        </ul>
    </nav>
}
